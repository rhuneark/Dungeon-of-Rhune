import Modal from './Modal.tsx';

export default function PortalPanel() {
    return (
        <Modal title="The Portal">
            <p className="text-sm text-white/70">
                A shimmering, faintly judgmental doorway. Real-time co-op runs are coming in a future update — for
                now it hums quietly and does nothing, which is more than can be said for you on floor 1.
            </p>
        </Modal>
    );
}
